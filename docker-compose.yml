services:
  docker-rss:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8083:8083"
    environment:
      - UPDATE_SCHEDULE=* * * * *
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
